# edp-quiz
# EDP Quiz (FastAPI + Telegram)

Very small project: FastAPI quiz about Event Driven Programming. When a student submits answers the server computes the percent and posts a message to a Telegram channel.

See `.env.example` — copy to `.env` and provide TELEGRAM_TOKEN and TELEGRAM_CHAT_ID.
